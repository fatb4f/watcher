#!/usr/bin/env bash
set -euo pipefail

mkdir -p "$HOME/.local/state/codex/watcher"
touch "$HOME/.local/state/codex/watcher/codex-alerts.jsonl"
exec tail -n 200 -F "$HOME/.local/state/codex/watcher/codex-alerts.jsonl"
